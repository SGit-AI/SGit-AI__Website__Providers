# Generated by bin/sync-providers.py — do not edit by hand.
#
# Each provider's `patterns:` block is copied verbatim from that site's own
# published markdown twin. Its claim counts come from that site's published
# claim index. This hub adds nothing except the family metadata.
synced: 2026-09-08
providers:
  - name: ElevenLabs
    canonical: https://elevenlabs.providers.sgit.ai
    live: https://sgit-ai.github.io/SGit-AI__Website__Provider__ElevenLabs
    kind: voice · text to speech
    summary: "Text to speech with character-level timestamps. Scoped keys, no per-key spend limit, and the first render's four failures."
    state: live
    site_version: v0.3.0
    state_chips: [verified, measured, docs]
    claims:
      docs: 14
      measured: 8
      projected: 3
      spec: 2
      unrun: 8
      verified: 14
    patterns:
        - provider: ElevenLabs
          product: Text to speech (REST)
          server: "Yes — ours, until sg.tts ships"
          p0:
            verdict: never
            note: "CORS allows it; the account quota is the only bound"
          p1:
            verdict: no
            note: "no per-key spend limit exists"
          p2:
            verdict: yes
            note: "only with a server we run"
          p3:
            verdict: spec
            note: "sg.tts specified; terms file in the vault"
        - provider: ElevenLabs
          product: Agents (conversational)
          server: "Yes — the vendor's minter holds your key"
          p0:
            verdict: never
            note: "the vendor forbids it explicitly"
          p1:
            verdict: no
            note: "no bounded key to mint"
          p2:
            verdict: yes
            note: "vendor signed URL, 15 min; or a hostname allowlist — not both"
          p3:
            verdict: na
            note: "not attempted"
  - name: OpenRouter
    canonical: https://openrouter.providers.sgit.ai
    live: https://openrouter.providers.sgit.ai
    kind: models · one API, many models
    summary: "Provisioned keys with a spend limit and a reset — the one provider in this family that can do pattern one. Not built yet."
    state: planned
    state_chips: [spec]
